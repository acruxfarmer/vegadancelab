# Vega operational build history — 21 September 2026

Pops account through the current development-build handoff, before the parent's planned source commit. This is descriptive history, not current requirements or release approval. Starting repository main was `ac4f6ca9c4dd50ede2925b0adca4546f0ece32dd`; no new committed or deployed revision is asserted at this cutoff.

Joe directed the team to treat the established foundation as settled and build the operational application: Vercel front door, worker, core workflows and a complete functional wireframe. He authorized ordinary implementation, testing, correction and development deployment without new internal gates. Square had to remain disabled until its downstream path was verified and ready. Production, destructive changes, spending/ownership changes and genuine human authentication remained outside autonomous continuation. This direction narrowed the work to application delivery; it did not reopen provider selection or authorize Production.

Rooster reconciled the prior hosted ingestion evidence with the source state rather than repeating foundation probes. Archie translated the existing Revision 7/module contracts into an operational implementation addendum. Astra added authenticated tenant-native commands, serialized Business state and idempotency receipts; Ray built member/staff surfaces and a deliberately labeled synthetic wireframe. Wider passes, video, events and merchandise surfaces remained illustrative or honestly empty, not completed domain integrations. The worker added durable processing receipts and immutable financial observations without interpreting payment as attendance, reservation authority or outbound provider action.

Three additive database changes were applied to the development project: `db/application-schema.sql`, `db/worker-schema.sql` and `db/application-processing-visibility.sql`. The application and worker roles were created without login pending protected credential handoff. Existing ingestion authority stayed intact. Operator-owned merchant bindings subsequently enabled staff-only processing metadata visibility, preserving member and cross-Business denial. The server role's scoped inbox SELECT is broader than the metadata returned by the API; raw payload does not appear in that API projection.

Buttons found that the first member projection exposed targeted staff message drafts. Astra corrected it with explicit collection projection and published-only notification visibility. Test Smoke independently demonstrated that waitlist promotion admitted an already-started class despite the ordinary reservation guard; Astra aligned promotion with the occurrence availability rule. Both corrections received passing regression evidence. These were implementation corrections under the existing objective, not new scope decisions.

Test Smoke's bounded result recorded 53 passing local tests, including all then-existing Node test files and a fresh ephemeral HTTP server exercising the four front-door assets and security headers. Actual Supabase role/RLS exercises proved the tested app/worker boundaries and staff merchant scope; synthetic fixtures and temporary SET ROLE capability were rolled back. The later four operational-handoff tests were reported by Astra; a final aggregate suite count was not yet provided at this history cutoff. Local component, database-policy, browser and hosted-user-journey evidence remain distinct.

The historical chain stops before hosted completion. Astra reported locked Bitwarden, a saved Vercel authentication token rejected with 403 and no available deployment MCP capability; these prevented protected application/worker handoff and Vercel deployment at this cutoff. The development Auth user count was reported as zero, so an initial verified identity is still needed before an explicit staff assignment. No authentication user was created. No new release, scope approval or owner risk acceptance is inferred from the implemented code or passing tests. Square remained disabled.

## Traceable professional records

- `rooster-operational-resume.md`: current instruction, starting repository and prior intake evidence.
- `operational-application-architecture.md`: Archie's contract and retained provider/authority boundaries.
- `design.md`: Ray's surface implementation, simulation boundaries and browser evidence.
- `security-privacy-operational-build.md`: Buttons' findings, corrections and data-access limits.
- `test-smoke-operational-build.md`: executed tests, SQL exercises, correction evidence and unsupported claims.
- `hosted-ingestion-verification.json`: earlier hosted intake evidence, not proof of today's downstream application.

Ray subsequently supplied `screenshots/verification.json`: ten checks passed with no recorded errors using bundled Playwright and the installed Edge fallback. The synthetic wireframe exercise covered member search/reservation/cancellation, staff check-in/undo, synthetic class/participant creation, all member/staff surfaces, 390-pixel mobile layout without horizontal page overflow and clearing access when leaving the workspace. Desktop and mobile screenshots were retained. The evidence expressly excludes authenticated backend or provider effects; it extends the local UI history without establishing a hosted operational journey.

Ray reported directly reviewing the 1440×1050 desktop and 390×844 mobile images. Mobile search/layout and CSP style issues were corrected before the complete passing browser run. This correction and retest is attributed to Ray's design record and browser evidence, not inferred from screenshot existence alone.

The eventual source commit remains outside this pre-commit cutoff. No foundation re-audit or secret values are reproduced in this history.
